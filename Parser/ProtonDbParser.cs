namespace ProtonDB_Parsing.Parser
{
    public class ProtonDbParser
    {
        // ProtonDB uses dynamic javascript pages that can't be easily parsed
        // This is going to take some time
        
        // Will probably have to use something like PhantomJS to mangle the DOM
        // Seems way overkill to extract two strings from a webpage, but oh well
    }
}